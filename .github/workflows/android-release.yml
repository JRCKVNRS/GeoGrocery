name: Build Play release (AAB)

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  bundle:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK packages
        run: sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build release AAB
        # Offset keeps versionCode comfortably above any earlier hand-set value (1).
        run: ./gradlew :app:bundleRelease -PversionCode=$((100 + ${{ github.run_number }})) -PversionName=1.0.${{ github.run_number }} --stacktrace

      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: geogrocery-release-aab
          path: app/build/outputs/bundle/release/app-release.aab
          if-no-files-found: error
